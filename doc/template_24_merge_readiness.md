# template_24 远程交付与 main 合并评估

## 结论

本次交付在 `codex/neon-city-template-24` 分支，基于 `origin/main` 的 `6dd88f8f004642aaa941ef86f20ba0d48846ca0e`。

当前未发现本次模板的未解决合并阻断，可以合并到 main。实际合并不在本次授权范围，未执行合并、生产构建或部署。若 main 后续变化，应重新检查冲突与实际受影响测试。

## 交付范围

- template_24：18 个生产版式、封面、9 项原创固定素材及列表注册。
- 独立构建器、34 项模板专项测试、固定数据 Worker 与浏览器验收工具。
- 开发说明、Goal 规划、G8 报告、G9 人工确认记录、机器规格和可审查摘要。
- 不提交其他模板的未跟踪改动、临时 API 启动器、运行日志、源 PPTX 或大体积本地导出物。

## 审查修正

1. 修复构建器未显式传 `--stage` 时忽略输出路径的问题；使用临时目录先复现失败，再增加回归测试证明不会覆盖正式文件。
2. 浏览器换图检查改为调用编辑器实际使用的 `getImageReplacementProps`，移除测试自行复制的裁切算法。操作仍由脚本调用 store，不宣称完成全程鼠标上传流程。
3. G8 归档器在 G9 已闭合后拒绝覆盖原确认和进度记录；有隔离测试验证原文件字节不变。
4. 正式模板 JSON 固定 LF，避免 Windows 自动换行使确定性构建的字节比较失效。

这些修正不改变已确认的正式模板 JSON、封面或九项素材，原 G9 确认仍对应同一视觉与内容产物。历史冻结清单不改写，新工具哈希和测试证据单独记录。

## 验证

| 检查 | 结果 |
|---|---|
| 模板专项、公共渲染、资源测试及相邻 template_23 可运行用例 | 132 passed，1 deselected |
| 18 页编辑器验证 | 完整 JSON 保存重载一致，全文/图片数量保留，换图函数、导出失败恢复和 PPTX 重新导入通过 |
| 正式模板与素材 | 与 G9 确认时哈希一致，构建器输出字节一致 |
| 本次独立审查 | 1 个构建器 P2 已修复；换图覆盖差距已补充；无未解决 P1/P2 |
| 远程 main | 提交时核对分支基线与冲突；不自动合并 |

扩展检查初次运行发现 `test_template_23_reference_registration_and_cover_are_available` 因桌面缺少“星空风格(1).pptx”失败。该测试与远程 main 的代码完全一致，失败来自已有的本机参考文件依赖。本次不修改该测试或制造假附件；复核运行明确排除这一项，不声称后端全量测试通过。

## 验证边界

Worker 使用真实 Worker/Handler/Renderer 和隔离 SQLite，但文本上游为固定数据。浏览器身份为夹具；没有验证生产 SSO、计费、在线文本模型、云端保存或远程部署。PPTX 重导入的字体主题、字重和细线呈现可能变化，不承诺逐像素一致。这些边界已在 G8 报告中说明。

仓库当前没有提交 `.github` CI 工作流；查询时 main 未启用分支保护。可合并结论来自本次本地验证与代码审查，不代表远程 CI 已运行。
