# 深蓝霓虹城市项目策划 PPT 模板开发 Goal 执行提示词

> 执行状态：`G9_COMPLETE`。用户已明确回复“确认完成”，候选 `template24-g8-f1fdf1cdaaa0` 已完成人工确认和闭合归档。详见[G9 闭合记录](./深蓝霓虹城市项目策划PPT模板G9闭合记录.md)。
> 阶段编号以用户本次粘贴的执行提示词为准：G1 素材、G3 MVP、G5 专项测试、G6 生产注册、G7 功能 QA、G8 最终核验。

## 使用方式

只有用户以后明确发送下面的“主执行提示词”或等效的“开始执行这个模板 Goal”指令，才可以创建并执行 Goal。

未来发送主执行提示词时，授权范围包括：

- 在 `D:\moling\TrainPPTAgent` 执行 G0～G8。
- 使用 GPT2 图片模板或其他授权明确方式生产本模板规划中的 9 项固定装饰图。
- 使用固定数据、Fakes/Mocks 和隔离环境完成模板专项测试和必要 QA。

该授权不包含 Git 提交、推送、PR、合并、部署、生产备份、迁移、计费开启或回滚。

## 主执行提示词

```text
请在 D:\moling\TrainPPTAgent 中创建并持续执行一个 Goal，完成“深蓝霓虹城市项目策划”PPT 模板的 G0～G8 开发任务。

Goal objective：
将 C:\Users\sk20\Desktop\项目策划(1).pptx 的深蓝城市夜景、蓝紫远景、青绿色光线和项目策划版面关系，重建为一套 TrainPPTAgent 生产模板。模板必须可选择、可生成、可编辑、可替换业务图片、可导出 PPTX、可重新导入，并具备固定数据回归测试。G8 全部通过后自动记录 G8_PASS / G9_PENDING；必须收到用户明确确认后才执行 G9 闭合。

不要设置 token_budget，除非我另行明确指定。

开始前必须读取并遵守：

1. AGENTS.md
2. doc\深蓝霓虹城市项目策划PPT模板开发说明.md
3. doc\深蓝霓虹城市项目策划PPT模板开发Goal.md
4. doc\深蓝霓虹城市项目策划PPT模板开发Goal执行提示词.md
5. doc\Template.md
6. doc\PPT_Structure.md
7. backend\main_api\workers\template_renderer.py
8. backend\main_api\template_assets.py
9. backend\main_api\tests\test_template_renderer.py
10. backend\main_api\tests\test_template_assets.py
11. 与当前相邻模板相关的 JSON、规格、专项测试和必要 QA 记录

基础范围：

- 模板名称：深蓝霓虹城市项目策划
- 候选模板 ID：template_24，必须在 G0 重新扫描；不要直接假定编号可用
- 参考 PPT：C:\Users\sk20\Desktop\项目策划(1).pptx
- 页面比例：16:9
- 逻辑画布：1000 × 562.5
- MVP：12 个版式
- 生产版：18 个版式
- 固定装饰：9 项
- 页面类型：cover、contents、transition、content、end

一、执行原则

- 严格按 G0 → G1 → G2 → G3 → G4 → G5 → G6 → G7 → G8 顺序执行。
- G0～G8 连续自动推进，不设置逐阶段、逐页面、逐素材或逐测试人工确认。
- 只保留三个内部检查点：C0 覆盖 G0～G1，C1 覆盖 G2～G6，C2 覆盖 G7～G8。每个检查点通过后自动进入下一组任务。
- 每个阶段都要检查输入、完成阶段任务、运行与风险匹配的验证、保存证据并记录状态。
- 普通测试失败、文字溢出、素材不合格、图片裁切错误和局部兼容问题，在范围内自动诊断、修复和重验。
- G2 三页探针未通过前，不扩展完整页面库存。
- 涉及公共渲染能力时，先增加固定数据失败测试，再做最小修复和受影响回归。
- 保留用户已有修改，不覆盖无关文件，不清理未跟踪用户产物。
- 不执行 Git 提交、推送、PR、合并、生产构建或部署。
- 不把源 PPTX 的占位文案、字体、动画、切换、图片、水印、网站和示例数据当作需求或生产资产。

二、GPT2 图片模板与 9 项素材

本 Goal 允许使用 GPT2 图片模板生成固定装饰图，例如可用的 gpt-image-2 图片生成能力。

- 必须调用当时可用的图片生成技能或工具，不能用提示词或文字描述冒充图片结果。
- 工具未暴露实际模型名称时记录“工具未暴露”，不得猜测或伪造模型名称。
- GPT2 图片模板不可用时，可以使用其他授权明确的图片来源，并记录真实来源。
- 每项素材单独生成，每项最多生成或重试 3 轮，达到规格和实际使用要求后停止。
- 保存提示词、工具信息、实际模型或“工具未暴露”、原始输出、最终文件、尺寸、格式、Alpha、体积、SHA-256、引用页面和检查结果。
- GPT2 图片模板只覆盖 A1～A9 固定装饰，不扩展到业务图片、用户照片或规划外素材。
- 素材不得包含文字、数字、Logo、网站、人物、真实品牌、真实地标、签名或水印。

固定素材为：

1. template_24_asset_bg_cover_v1.jpg，RGB JPEG，1920×1080
2. template_24_asset_bg_content_v1.jpg，RGB JPEG，1920×1080
3. template_24_asset_bg_section_v1.jpg，RGB JPEG，1920×1080
4. template_24_asset_bg_end_v1.jpg，RGB JPEG，1920×1080
5. template_24_asset_city_grid_blue_v1.jpg，RGB JPEG，1200×1200
6. template_24_asset_city_glow_purple_v1.jpg，RGB JPEG，1200×1200
7. template_24_asset_route_light_v1.png，RGBA PNG，1800×460
8. template_24_asset_edge_beam_v1.png，RGBA PNG，650×1080
9. template_24_asset_skyline_corner_v1.png，RGBA PNG，1200×900

三、固定页面库存

MVP 必须包含：

cover-city
contents-2
contents-3
contents-4
contents-5
contents-6
contents-10
transition-city-left
content-text-2
content-text-3
content-text-4
end-city

生产版在 MVP 基础上增加：

cover-city-minimal
transition-city-right
content-focus-1
content-image-1
content-metrics-4
end-action

生产版必须精确包含 2 个 cover、6 个 contents、2 个 transition、6 个 content、2 个 end，合计 18 个版式。不得在执行中自由增加页面库存。

四、G0～G8 任务

G0：预检

- 记录工作树、当前分支和用户已有修改。
- 核对参考文件存在并计算 SHA-256。
- 扫描模板注册表、模板 JSON、封面、素材和测试目录，确认可用模板 ID。
- 如果候选 ID 冲突，同步更新 Goal、开发说明、规格、构建器、资源、测试和注册文件名。
- 确认现有渲染器、资源处理、前端解析和测试入口。

G1：参考页与素材

- 将源 PPTX 25 页逐页记录为采用、派生、重建或排除。
- 记录图片、字体、动画、切换、网站、签名和水印的处置结论。
- 按 A1～A9 生产或准备装饰图，并把记录保存到 doc/assets/template_24_qa/。
- 检查 RGB/RGBA、Alpha、尺寸、体积、清晰度、低干扰区和引用页。
- 源 PPTX 中的业务照片和示例图片不得直接进入正式素材目录。

G2：三页技术探针

- 完成 cover-city，验证标题编辑、装饰锁定和导出往返。
- 完成 content-text-4，验证四项正文、长标题和无损分页。
- 完成 content-image-1，验证横图、竖图、方图裁切、换图和装饰保护。
- 三页必须能够导入、显示、编辑、保存重载、导出并重新导入。
- 失败时先写固定数据失败测试，再最小修复和重验。

G3：规格和 MVP

- 创建机器可读规格文件和确定性模板构建器。
- 完成 12 页 MVP，所有页面、元素和分组 ID 全局唯一。
- 标题、正文、编号、指标、线条和普通图标使用可编辑元素。
- 业务图片使用 imageType=content；固定背景和装饰使用 imageType=decoration。
- 连续执行两次构建器，输出结构必须等价。

G4：代表性样稿

- 完成 cover-city、contents-4、transition-city-left、content-text-4、end-city 五张样稿。
- 检查色板、留白、文字对比度、标题容量和装饰边界。
- content-image-1 已由 G2 单独验证，不重复计入五张样稿。

G5：语义和专项测试

- 检查页面类型、textType、groupId、imageType、sourceTitle 和资源地址。
- 检查目录 2/3/4/5/6/10 项，正文 2/3/4 项、单重点、单图文、四指标和行动页。
- 检查长标题、正文分页、图片替换、装饰保护和错误路径。
- 原始标题、正文和项目顺序必须完整保留；不得静默截断或丢弃尾部项目。

G6：生产版与注册

- 增加 6 个生产版式，形成 18 页生产 JSON。
- 生成模板选择器封面。
- 注册模板列表和静态资源。
- 检查生产页面、资源、封面和注册项一致。

G7：真实功能和设备检查

- 在固定数据和授权范围内完成真实生成、编辑标题、编辑正文、保存重载和业务图片替换。
- 检查失败反馈、重试入口和导出状态；不把失败当作成功。
- 若本次修改了模板选择页或编辑器外壳，检查 1920×1080、1366×768、768×1024、390×844 四种视口。
- 未修改前端入口时，不扩大为全站响应式整改。

G8：冻结候选与最终 QA

- 运行模板专项测试和实际受影响公共回归。
- 导出 PPTX，检查页数、标题、正文、图片、裁切、装饰和编辑后的值。
- 使用项目同款导入链路重新导入，保存页面和元素统计摘要。
- 保存素材哈希、测试输出、截图、PPTX、重导入摘要和已知限制。
- 全部通过后自动记录 G8_PASS / G9_PENDING，并提交候选版本和验收证据；不得自动闭合 Goal，必须等待用户明确确认后执行 G9。

五、完成条件

- 18 页生产模板结构完整，页面类型和数量准确。
- 9 项固定装饰全部有实际生产页面引用。
- 模板 JSON、封面、资源和注册项一致。
- 标题、正文、目录、指标、行动文字和项目顺序完整保留。
- 业务图片可替换，固定装饰不会被覆盖。
- 长标题不会被静默截断，正文超量按原顺序分页。
- 固定数据专项测试和受影响回归通过。
- 当前候选完成编辑、保存重载、图片替换、导出和项目同款重新导入。
- G8 通过后保持 G9_PENDING；不得自动标记 Goal 为 complete。收到用户明确确认后才可以执行 G9 闭合。不得因此执行 Git 交付、部署或生产操作。

六、证据和报告

每个阶段保存最小但可复查的证据：

- G0：源文件哈希、模板 ID 扫描、工作树基线。
- G1：逐页映射、素材来源、GPT2 工具信息或“工具未暴露”、素材检查。
- G2：三页探针截图、编辑换图记录、PPTX 往返摘要。
- G3～G6：规格、构建器、模板 JSON、页面总览、专项测试。
- G7：真实生成、编辑、换图和四视口证据（仅在实际影响范围内）。
- G8：冻结候选清单、测试结果、PPTX、重新导入摘要和已知限制，并记录 G8_PASS / G9_PENDING 状态。

最终报告必须说明：完成了什么、哪些证据通过、哪些问题被排除在范围外、当前候选是否为 G8_PASS / G9_PENDING，以及等待用户确认的 G9 闭合动作；Git、部署或生产动作仍需要单独授权。
```

## 当前状态

用户已以粘贴提示词明确启动本 Goal；G0～G8 已通过，用户随后回复“确认完成”，G9 已完成闭合归档。

本文件保留执行提示词供追溯，不代表再次创建 Goal 或重复已经通过的开发检查。续接任务先读取执行报告与当前候选证据。
