# template_26 远程交付与 main 合并评估

## 结论

**可以合并到 main。** 功能提交 `eef27f579771a3854ed2ba046c7cd564dd908204` 已推送到 `origin/codex/deepblue-circuit-security`，GitHub API 已核对远程与本地提交一致。

评估基线为远程 `main` 的 `a35e430302b5efc8672a82f214c9c06fae594477`。功能提交领先 1 个提交、落后 0 个提交，`git merge-tree --write-tree origin/main HEAD` 无冲突，main 是功能提交的祖先，可快进合并。本次随后保存评估的文档提交不改变已验证代码。

当前无已确认的合并阻断。仓库 main 未启用分支保护，生效分支规则为 0，Actions 工作流、该功能提交的 check runs 和 commit statuses 均为 0。因此结论依据代码审查及本地验证，不代表远程 CI 通过。远程 main 或分支规则变化时应重新评估相应影响。

## 本次提交范围

正式的 22 个可编辑版式、封面及 8 项固定素材、确定性构建器、模板注册、独立指标单位支持、相关测试、验证工具、规划和必要证据。只按文件白名单暂存，不包含其他模板未跟踪产物、原始 PPT、原始生成文件、大体积导出物、逐页截图、运行日志或 `.env`。

用户确认候选 `template26-5db890c046cf` 的记录原样保留，正式 JSON、封面和 8 项素材未改变。新克隆直接使用仓库内正式素材，构建器不依赖原验收机的生成文件。

## 合并前修正

1. 后端显式声明 `metricUnitField=unit` 的指标页保留独立单位，空单位不删除业务组；容量或图片触发普通正文回退时，数值和单位同时保留。
2. 旧前端本地生成器对声明独立单位协议的库存支持数值、单位、名称、说明分别填充，保留零值及原始顺序；数量不匹配时按普通文字库存分页，普通正文不误选指标示例页。旧未声明协议的模板保持原行为。
3. 标注面板增加“指标单位”选项。本地旧入口的带业务图指标输入会显示明确错误，要求使用云端生成或拆成图文页，避免成功返回却丢图；后端 Worker 的图文回退不受此限制。
4. QA 新运行先使旧 PASS 失效；依赖加载或验证失败后不得沿用旧成功摘要。归档工具在用户已确认后拒绝覆盖候选，并检查前端结果、类型检查及 JUnit 跳过项。
5. 为 `template_26.json` 固定 LF，保持跨平台确定性构建。

上述修正不改变已确认版式、配色和正式素材，也不改变认证配置。本次没有启动关闭 SSO 的测试服务。

## 验证

| 检查 | 结果 |
|---|---|
| 后端模板、公共渲染、资源及 template_21/22/24/25 回归 | 323 passed，无失败或跳过 |
| 前端独立指标、图片协议、导出状态 | 31 passed |
| TypeScript 类型检查 | 通过 |
| 仅含暂存文件的独立副本 | 后端同样 323 passed，前端 31 passed，类型检查通过 |
| 暂存副本重建 | 模板 JSON 与正式文件按字节一致 |
| 真实编辑器旧入口复验 | 四项独立指标、三项及十三项回退通过；带图指标错误提示可见 |
| 原确认成果 | 正式 JSON、封面及 8 项素材哈希不变；T8 人工确认和闭合记录保留 |
| 提交范围 | 明确文件白名单，常见凭据模式检查未发现匹配 |
| 合并及远程核对 | 功能提交已在远程，无冲突，behind=0 |

原 T8 的 22 页编辑、换图、保存、PPTX 往返、隔离 Worker 与持久化、四类设备和 PowerPoint 原生渲染证据可复用；未把这些历史通过记录冒充本次重新执行。

## 授权与证据

本次授权覆盖提交、推送和合并判断；**没有创建 PR，没有实际合并 main，没有生产构建或部署**。

- [验收与复现说明](./assets/template_26_qa/README.md)
- [交付检查摘要](./assets/template_26_qa/ship-verification.json)
- [工作区回归](./assets/template_26_qa/ship-tests.xml)
- [暂存副本回归](./assets/template_26_qa/ship-staged-tests.xml)
- [本地指标浏览器检查](./assets/template_26_qa/ship-local-metrics/summary.json)
- [远程核对快照](./assets/template_26_qa/ship-remote-after.json)
- [原人工确认记录](./assets/template_26_qa/closure.json)
