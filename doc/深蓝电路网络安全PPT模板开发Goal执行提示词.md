# 深蓝电路·网络安全 PPT 模板 Goal 执行提示词

> 当前状态：提示词已编写，尚未启用。本次仅保存 Markdown 文档，不创建实际 Goal、不执行开发或图片生成。
> 使用方式：后续决定开始开发时，将下面代码块的完整内容作为新的执行请求发送。它要求执行开发；仅阅读、编辑或保存本文不构成启动。

关联文档：[开发说明](./深蓝电路网络安全PPT模板开发说明.md)、[开发 Goal 规划](./深蓝电路网络安全PPT模板开发Goal.md)。

```text
请在 D:\moling\TrainPPTAgent 中创建或继续“深蓝电路·网络安全 PPT 模板开发”这一单个 Goal，并执行以下规划，完成模板开发及验收。

一、先读取范围内文档

1. D:\moling\TrainPPTAgent\AGENTS.md
2. D:\moling\TrainPPTAgent\doc\深蓝电路网络安全PPT模板开发说明.md
3. D:\moling\TrainPPTAgent\doc\深蓝电路网络安全PPT模板开发Goal.md

本次执行请求授权开始上述范围的开发、必要的本地验证，以及下述图片生成。规划文档中“本次只编写文档、开发未开始”等文字记录的是编写时状态，不作为本次执行的禁止项。

T1～T7 自动判断、检查、确认并推进；范围内实现选择、素材生成和普通缺陷修复自行处理，不等待逐项审批。T8 自动检测无问题后必须提交当前候选等待人工确认，收到明确确认才闭合并完成 Goal。自动检查必须依据实际结果，不能把功能失败或未执行检查记为通过。

使用一个 Goal 覆盖 T1～T8，不另改阶段编号或拆成逐页 Goal。先检查是否已有同一目标；已有则继续，避免重复创建。遵循 Goal 工具的实际约束，不覆盖其他未结束目标，不自行添加 Token 预算。若环境没有 Goal 能力，明确说明并用任务表记录进度，不声称已创建实际 Goal。

二、交付范围

- 模板名称：深蓝电路·网络安全。
- 模板 ID：开工时核对占用后确定 template_<N>，不假定为某个固定编号。
- 画布：1000 × 562.5，16:9。
- 基础版：14 个版式；完整版：在基础版上增加 8 个，共 22 个。
- 页面 ID、容量、图片槽和素材引用以开发说明第 2 节为准，不自行缩减或扩展。
- 固定素材：A1～A8，共 4 张背景、4 项透明装饰；列表封面和业务样例图不计入这 8 项。
- 完成模板选择、内容填充、编辑、换图、裁切、保存重开、PPTX 导出及重新导入，以及受影响入口的设备适配。
- 不复刻原稿音乐、动画、特殊字体和数据驱动图形，不进行无关公共重构或全站整改。

三、图片生成方式

模板所需图片可以使用 GPT2 图片模板生成。这包括 A1～A8 的背景、锁形、电路和网络球，以及确有需要的业务展示样例图；按开发说明第 3 节的尺寸、格式、构图和透明通道要求制作。

在这个已授权范围内按需要分批生成，不逐张请求确认。可从统一基础画面派生素材，放入实际页面检查，达到使用要求后停止迭代，不为主观相似度反复重做。

业务展示样例图与固定装饰分开管理，不替代用户业务图片。文字、数字、流程节点和业务关系必须保持可编辑，不能用整页图片代替。

记录素材来源、版本、尺寸、用途和检查结果。实际模型名称以工具返回为准，未暴露时记录“工具未暴露”；不要声称已验证底层模型就是 GPT2，不因模型名称无法证明而重复生成合格素材。

内容填充和分页测试使用固定输入及必要的 Fake/Mock，无需调用真实文本或大纲生成模型、运行生产任务。该测试方式不限制这里已明确授权的图片生成。

四、按 T1～T8 推进

T1：检查工作区已有修改并保留；确定模板编号、字体、边距和容量。未变化的源文件检查直接复用。
T2：分批制作 A1～A8，与页面实现交错检查。已合格素材直接使用，其余素材继续制作，不阻塞无关页面；最终交付仍包含 8 项。
T3：编写可重复运行的构建器，实现 14 个基础版式，保证对象、槽位、页面 ID 和资源引用正确。
T4：接入选版、内容槽、图片角色、标题适配和无损分页，贯穿 T3 与 T5；逐批自动验证并记录，不设整项放行点。
T5：补齐长标题封面、双图文、三图文、四图文、四步骤流程、四项指标、六节点关系和行动结束页，共 22 个版式。可以与基础版实现和检查交错进行。
T6：随可运行候选在项目本地接入模板名称、ID、JSON、实际渲染的封面及资源，检查可选择、可加载；最终整理完整正式集合。注册模板不等于部署生产。
T7：验证改字、换图、裁切、保存重开、PPTX 导出和重新导入；发现普通缺陷后修复并重验。
T8：自动检测受影响入口在桌面、笔记本、平板和手机的可用性、修改按钮反馈及交付结果；无问题后准备当前候选及证据，进入“待人工确认”。用户明确确认当前候选完成后，才执行闭合并完成 Goal。

T1～T8 是工作分类，不是严格串行的审批阶段。先核对编号和工作区，其余步骤按实际依赖自行调度；有可构建页面就实现，有可运行样例就检查，只有直接依赖的输入缺失时才暂缓对应步骤。
T5 不等待基础版集中验收，T6/T7/T8 的检查可以提前穿插。T1～T7 检查通过后自动确认对应工作完成并继续；T8 检测通过只进入待人工确认，不直接完成 Goal。最终交付仍为 22 个版式，不在 14 个版式时结束 Goal。

五、必须保留的内容和交互规则

- 原始标题、正文、数字、单位和项目顺序不得丢失。展示标题适配时保留 sourceTitle，并在可见内容中完整保留原题。
- 目录超过容量和正文过长时保序分页；不能仅因条目标题长，将四项页拆成四个单项页。
- 业务图标记 imageType=content，固定背景和装饰标记 imageType=decoration。换图不得覆盖装饰，横图、竖图和方图不得拉伸。
- 多图与条目一一对应，数量不匹配时明确反馈，不忽略图片或复制凑数。
- 专项布局不适合时由续页或普通正文完整承接，保留业务关系和说明；不补造数据。
- 业务文字、指标、流程节点和关系图保持可编辑，A8 网络球只作装饰。
- 代码中的非显然逻辑用中文注释说明。前端受影响入口适配四类设备，新增或修改的按钮提供明确反馈。

六、验证、续作和完成

按开发说明第 6 节执行必要检查，按 Goal 规划第 6 节汇总最终交付结果；检查可以合并，不转换为逐项审批。只扩大到实际受影响的行为；发现缺陷保留复现样例或失败测试后自动修复重验，同时继续独立工作。输入、代码和环境未变化时复用有效证据。

T1～T7 完成后自动更新两份规划中的任务状态，记录当前候选、产物路径和必要检查结果。T8 自动检测通过后记为“待人工确认”，收到明确确认后才更新为已完成；确认依据直接记录在任务文档中，不要求另建审批文件。恢复执行时核对已有记录和实际候选，从未完成项继续，不重复生成合格素材或重复通过的检查。

若需运行候选程序，先核对代码版本、工作目录、监听端口、PID、命令行及项目归属；只启动或重启必要服务，健康的旧分支程序不能代替当前候选验证。

范围内工作不逐页、逐素材或逐测试请求确认，T8 最终人工确认是唯一例外。审美偏好、底层模型名称未暴露和其他范围外事项不阻塞前期开发。除最终人工确认外，只有缺少不可替代输入、无法自行解决的外部问题或范围外授权需求才请求用户处理；此前继续独立工作，遵循 Goal 工具状态规则。

自动汇总三类结果：一是 22 个版式和 8 项素材完整有效；二是内容保真、编辑、图片处理、保存及 PPTX 往返等核心流程可用；三是设备适配和交付物齐全。全部检测无问题后，准备可检查的程序或样例、导出文件及结果，说明模板编号和候选标识，再请用户确认该候选是否完成。

等待答复期间将 T8 保持为“待人工确认”，保持 Goal 未完成，不执行闭合。用户明确回复“确认完成”或等效验收意见后，记录确认依据及对应候选，更新 T8 为已完成，再完成实际 Goal。沉默、超时、自动检测通过或此前一般性的开发授权都不能替代这次人工确认。

用户反馈问题时继续修复，重验受影响部分并更新候选，再次提交人工确认；未变化部分复用有效证据。闭合仍归入 T8，不新增 T9，只更新验收记录、任务状态和 Goal 状态，不自动执行 Git 交付或生产操作。

最终报告模板编号、完成库存、素材状态、检查结果、交付文件和实际限制；运行过候选时给出已验证的本地地址。不要把“代码写完”当作全部完成。

本次执行不包含 Git 提交、推送、PR 创建、合并、生产构建、部署、迁移、备份、计费开启或回滚；这些动作等待对应的明确授权，不作为开发 Goal 的完成前置条件。
```
