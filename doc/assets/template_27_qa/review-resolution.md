# template_27 审查与修复记录

本次审查基线为 `180d26961e05cf6a5d855c37d96653b4ab0963a6`，包含本任务明确新增的文件和工作区修改；不包含其他未跟踪产物。审查后进行了只读复核，没有提交、推送或生产操作。

## Standards

初次审查发现一项验证缺口：往返检查只比较文字和图片，未检查流程和时间轴的节点、连接线。实际重放后，第 18 页应有 4 条原生线，导入得到 0 条线和 4 个零高度图形，截图确认连接线不可见。

修复前反馈命令：`node utils/verify_template_27_connectors.cjs`，结果为 `第18页原生连接线丢失：应至少4条，实际0条；退化图形4个`。导出文件中的水平线为高度为零的 `custGeom`。新增 `useExportLines.spec.ts` 后，六种直线用例失败，折线与曲线等五项原行为检查通过。

修复：普通直线改为原生 `line`，按端点设置位置、尺寸与翻转方向；保留原有线型、箭头和曲线路径。浏览器往返增加 82 个原生图形和线条的类型及几何检查，第 18、19 页分别保留 4、5 条线，并通过真实键盘移动验证。独立只读复核确认原发现已解决，未发现新的具体回归。

## Spec

初次审查发现一项验收缺口：换图测试后恢复了原图，保存稿也没有实际修改节点和指标数值，原 PASS 不足以证明这些编辑能保存。

修正：正式示例在编辑测试前独立导出；破坏性 QA 副本通过键盘修改流程标题、时间轴标题、指标数值 `128` 和单位，并保留一张不同裁切比例的业务图片替换。随后执行完整 JSON 保存重载和隔离 SQLite API 持久化比对，均通过。独立只读复核确认该发现已解决。

封面字段另作明确说明：副标题和汇报人、日期等补充信息由多行 `data.text` 承载，已有独立固定测试及真实显示边界样例，不增加业务字段。

## 图片读取失败补充验证

用户要求覆盖图片读取失败。新增 `useExportFailure.spec.ts` 后发现读取 404 图片会直接抛出 `Fetch failed: 404`，发生在原写出异常处理之外。修复将文档构建与写出统一纳入导出入口的 `try/catch/finally`，保证反馈“导出失败”并复位加载状态。

`image-failure-summary.json` 是真实浏览器对不可读图片的验证，确认统一错误码、可见提示、状态复位和同一实例重试成功。相关前端回归共 27 项通过，类型检查通过。该修复只影响实际涉及的导出路径，没有扩展为通用导入器重构。

审查问题已经处理；P6 仍需用户对最终候选人工确认，本记录不代表 Goal 已闭合。

原生渲染环境已核实：本机 `PowerPoint.Application` 注册实际指向 WPS Office 的 `office6` 安装目录。原生截图和文字检查属于 WPS 兼容接口验证，不将它表述为微软 PowerPoint 桌面版验证；项目编辑器的真实 PPTX 导出与重新导入另有独立证据。
