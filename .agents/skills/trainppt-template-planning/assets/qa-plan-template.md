# {{template_name}} PPT 模板素材与 QA 计划

## 1. 规划状态

| 项目 | 内容 |
|---|---|
| 模板 ID | `{{template_id}}`，候选 |
| 规格路径 | `doc/template_specs/{{template_id}}.yaml` |
| QA 状态 | `NOT_RUN` |
| 文档性质 | 规划阶段待执行清单，不包含测试或运行结果 |

## 2. 素材 manifest

| ID | 角色 | 发布文件名 | 格式/尺寸 | Alpha | 最大字节 | 安全区 | 权利动作 | 最大尝试 |
|---|---|---|---|---|---:|---|---|---:|
| `{{asset_id}}` | {{role}} | `{{filename}}` | {{format_and_dimensions}} | {{alpha}} | {{max_bytes}} | {{safe_zone}} | {{rights_action}} | {{max_attempts}} |

提示词约束：

- {{prompt_constraint}}

规划阶段不填写生成工具结果、文件哈希或“已通过”。后续实施应记录实际工具暴露的模型信息；工具未暴露时写“未暴露”，不得猜测。

## 3. 自动化测试矩阵

| 案例 ID | 检查对象 | 输入边界 | 预期结果 | 证据类型 | 状态 |
|---|---|---|---|---|---|
| `{{case_id}}` | {{subject}} | {{input_boundary}} | {{expected_result}} | {{evidence_type}} | `NOT_RUN` |

至少规划：

- 模板 ID、画布、页面库存、MVP 标记与全局唯一 ID；
- 资源引用、发布集合、图片格式、尺寸、模式、Alpha 和体积；
- 模板列表唯一注册、JSON、封面和外置资源响应；
- 目录与正文容量精确选版；
- 0、1、允许上限和过量内容图片；
- 横图、竖图、方图裁切和缺失尺寸错误；
- 8 项以上与长正文无损分页；
- 带图长正文仅在首段保留内容图；
- 固定装饰不被内容图替换；
- 声明变体可由确定条件选中；
- 通用渲染器、素材和受影响回归。

## 4. 真实任务与交互 QA

| 案例 ID | 场景 | 操作 | 通过标准 | 计划证据 | 状态 |
|---|---|---|---|---|---|
| `{{case_id}}` | {{scenario}} | {{action}} | {{pass_criteria}} | {{evidence_path}} | `NOT_RUN` |

至少规划：

- 真实持久 Worker 生成成功；
- 模板选择列表唯一显示；
- 声明页面类型都在真实作品中出现；
- 编辑文字后保存并重新加载；
- 替换内容图且固定装饰不变；
- 异步失败有可见反馈，按钮恢复并可重试；
- PPTX 导出、解析、重导入并继续编辑。

## 5. 视口矩阵

| 案例 ID | 设备类别 | 宽 × 高 | 检查点 | 计划截图 | 状态 |
|---|---|---:|---|---|---|
| `{{viewport_id}}` | {{device_class}} | {{width}} × {{height}} | 无横向溢出；关键按钮可见、可触达且有反馈 | `{{screenshot_path}}` | `NOT_RUN` |

应覆盖桌面、笔记本、平板和手机；尺寸来自本次规格，不从历史证据复制。

## 6. PPTX 往返

| 案例 ID | 检查 | 通过标准 | 计划证据 | 状态 |
|---|---|---|---|---|
| `export-roundtrip` | 导出、解析、重导入与继续编辑 | 页数和核心结构合理，重导入后可编辑 | `{{roundtrip_summary_path}}` | `NOT_RUN` |

## 7. 完成条件覆盖

| 完成条件 ID | 案例 ID | 预期证据 |
|---|---|---|
| `{{criterion_id}}` | `{{case_id}}` | {{evidence_type_or_path}} |

所有完成条件必须映射至少一个真实案例；无法验证的条件不能从规格中隐藏。

## 8. 证据目录策略

标准入口：`doc/assets/{{template_id}}_qa/evidence.json`

默认计划提交：

- `evidence.json`；
- 生产模板和真实任务蒙太奇；
- 四视口关键截图；
- 编辑、换图、失败重试和重导入关键截图；
- 提示词、来源、授权和摘要。

默认不提交：

- 图片生成原图；
- 全部逐页原始截图；
- 大型 QA PPTX 和临时导出；
- 进程日志和包含用户数据的调试文件。

## 9. 待确认与已知限制

开放决策：

- {{open_decision_or_none}}

已知限制：

- {{known_limit_or_none}}

该文档在规划阶段不得把任何案例状态改为 `PASS`。实际结果由后续开发 QA Skill 和证据文件记录。
