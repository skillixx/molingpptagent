# 🚀 安装
```
npm install

npm run dev
```
Browser access: http://127.0.0.1:5778/

> 默认只允许 `ppt.axicomin.cn` 访问 Vite 开发服务器。如本地特殊联调需要任意 Host，显式设置 `VITE_ALLOW_ALL_HOSTS=true` 后再启动；生产环境应使用静态构建产物。

# 检查配置
vite.config.ts中配置的API地址
http://127.0.0.1:6800
